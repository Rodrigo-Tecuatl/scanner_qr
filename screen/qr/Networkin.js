import React from 'react';
import { SafeAreaView, Text, View } from 'react-native';

export default function Networkin({ navigation }) {
  return(
    <SafeAreaView>
      <Text>Información de Networkin</Text>
    </SafeAreaView>
  );
}