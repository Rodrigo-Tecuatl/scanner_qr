import React from 'react';
import { SafeAreaView, Text } from 'react-native'

export default function Index({ navigation }) {
	return(
		<SafeAreaView>
			<Text>Ingrsaste al indece del programa</Text>
		</SafeAreaView>
	);
}